set $SWAY_DIR $HOME/.config/sway

include $SWAY_DIR/variables
include $SWAY_DIR/colorschemes/catppuching-mocha
include $SWAY_DIR/outputs
include $SWAY_DIR/inputs
include $SWAY_DIR/startup

include $SWAY_DIR/keybindings/*
# include $SWAY_DIR/keybindings/basics
# include $SWAY_DIR/keybindings/apps
# include $SWAY_DIR/keybindings/movement
# include $SWAY_DIR/keybindings/workspaces
# include $SWAY_DIR/keybindings/scratchpad
# include $SWAY_DIR/keybindings/resizing

include $SWAY_DIR/waybar

include /etc/sway/config.d/*

output * bg ~/.config/sway/wallpapers/bjork.jpg fill
